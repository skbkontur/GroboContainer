<?xml version="1.0" encoding="utf-8"?>
<Project xmlns="http://schemas.microsoft.com/developer/msbuild/2003">

  <PropertyGroup>
    <Configuration>Release</Configuration>
  </PropertyGroup>

  <Target Name="Publish">
    <MakeDir Directories="Output" />
    <MSBuild Projects="GroboContainer\GroboContainer.sln" Targets="Rebuild" Properties="Configuration=$(Configuration)" />
    <Exec Command="Assemblies\ILMerge.exe /xmldocs /v4 /internalize /out:Output\GroboContainer.dll Assemblies\GroboContainer.dll Assemblies\GrEmit.dll" />
  </Target>
  
</Project>